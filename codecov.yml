codecov:
  notify:
    require_ci_to_pass: no

coverage:
  precision: 2
  round: nearest
  range: "70...100"

parsers:
  gcov:
    branch_detection:
      conditional: yes
      loop: yes
      method: no
      macro: no

comment:
  layout: "header, diff"
  behavior: default
  require_changes: no
